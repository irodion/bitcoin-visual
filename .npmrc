public-hoist-pattern[]=react
public-hoist-pattern[]=react-dom

# CI hardening: reject packages published less than 3 days ago
min-release-age=259200

# CI hardening: disable lifecycle scripts (postinstall, etc.) to block supply-chain attacks
ignore-scripts=true
